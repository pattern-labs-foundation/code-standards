name: Standards Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  standards:
    # @v1 latest rules in v1 | @v1.0.3 frozen | @main bleeding edge
    uses: pattern-labs-foundation/code-standards/.github/workflows/standards-review.yml@v1
    # with:
    #   fail_on_violation: false
    #   comment_on_pr: false
