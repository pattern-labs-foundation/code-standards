name: Standards Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  standards:
    # @main for latest, or pin a dated tag like @2026-09-13-v1
    uses: pattern-labs-foundation/code-standards/.github/workflows/standards-review.yml@main
    # with:
    #   fail_on_violation: false
    #   comment_on_pr: false
